// basic

int main() {
    int a, b = 3, c, d;
    if (b) {
        a = b*2;
        printf("true b\n");
    }
    if (a==b*2) {
        printf("a = b * 2;\n");
        c = a;
    }
    if (c == b) {
        printf("wrong\n");
    } else {
        printf("c != b;\n");
    }
    while (b > 0)
        b = b - 1;

    return 0;
}